В будущем - реализует валидацию всевозможных российских форматов номеров вроде ИНН, БИК, ОКАПО, КПП и прочего. Все что можно провалидировать.

class Account < ActiveRecord::Base
  validates_format_of_inn :inn
end
